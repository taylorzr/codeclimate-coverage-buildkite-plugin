#!/usr/bin/env bash

# This exists to mock the real cc-test-reporter. Notice this file is not executable, this is
# intentional because the plugin makes it executable

echo fake-cc-test-reporter called with "$@"
